# Security policy

This project is a dependency-free static site. It does not require API keys, accounts, analytics, uploads, or a backend.

The included preview script binds only to `127.0.0.1`, so it is not exposed to other devices on the network by default.

If you find a security issue, please use GitHub's private security advisory feature instead of posting credentials or sensitive details in a public issue.
